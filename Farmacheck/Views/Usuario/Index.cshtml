@model List<Farmacheck.Models.UsuarioViewModel>
@{
    ViewData["Title"] = "Usuarios";
}
<head>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/choices.js/public/assets/styles/choices.min.css" />
</head>

<div class="container py-4">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h4 class="text-dark">Usuarios</h4>
        <button id="btnNuevo" class="btn" style="background-color:#00ab8e; color:white;">
            <i class="bi bi-plus-circle"></i> Nuevo usuario
        </button>
    </div>
    <table class="table table-bordered custom-table" id="tablaDatos">
        <thead>
            <tr>
                <th style="width:10%;">Id</th>
                <th style="width:25%;">Nombre</th>
                <th style="width:25%;">Email</th>
                <th style="width:20%;">Teléfono</th>
                <th style="width:10%;">Estatus</th>
                <th style="width:10%;" class="text-center">Acciones</th>
            </tr>
        </thead>
        <tbody></tbody>
    </table>
</div>


<div class="modal fade" id="modalEntidad" tabindex="-1" aria-labelledby="modalTitulo" aria-hidden="true">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            <div class="modal-header bg-primary_form text-white">
                <h5 class="modal-title" id="modalTitulo">Nuevo usuario</h5>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Cerrar"></button>
            </div>
            <div class="modal-body">
                <input type="hidden" id="entidadId" />
                <div class="row">
                    <div class="mb-2 col-md-6">
                        <label>Nombre</label>
                        <input type="text" class="form-control" id="nombre" placeholder="Nombre" />
                    </div>
                    <div class="mb-2 col-md-6">
                        <label>Apellido paterno</label>
                        <input type="text" class="form-control" id="apellidoPaterno" placeholder="Apellido paterno" />
                    </div>
                    <div class="mb-2 col-md-6">
                        <label>Apellido materno</label>
                        <input type="text" class="form-control" id="apellidoMaterno" placeholder="Apellido materno" />
                    </div>
                    <div class="mb-2 col-md-6">
                        <label>Email</label>
                        <input type="email" class="form-control" id="email" placeholder="correo@dominio.com" />
                    </div>
                    <div class="mb-2 col-md-6">
                        <label>Número de teléfono</label>
                        <input type="text" class="form-control" id="telefono" placeholder="Teléfono" />
                    </div>
                    <div class="mb-2 col-md-6">
                        <label>Estatus</label>
                        <select class="form-select" id="estatus">
                            <option value="true">Activo</option>
                            <option value="false">Inactivo</option>
                        </select>
                    </div>
                </div>


                <div class="row mb-3">
                    <div class="col-md-12">
                        <button id="btnResetPassword" type="button" class="btn w-100" style="background-color: #00ab8e; color: white;">
                            <i class="bi bi-arrow-counterclockwise"></i> Reestablecer Password
                        </button>
                    </div>

                </div>
                <div class="row mb-3">
                    <div class="col-md-12">
                        <button id="btnGestionarRoles" type="button" class="btn w-100" style="background-color: #79b828; color: white;">
                            <i class="bi bi-gear"></i> Gestionar Roles
                        </button>

                        <select id="unidadDeNegocioSelect" class="form-select"></select>

                        <select id="marcasSelect" class="choices-dropdown" multiple></select>

                        
                    </div>
                </div>


                <div class="row mb-3">

                    <div class="col-md-6">
                        <label class="form-label">Seleccionar Rol</label>
                        <select class="form-select" id="selectRol">
                            <option value="">-- Selecciona un rol --</option>
                        </select>
                    </div>
                </div>

                <!-- Tabla -->
                <table class="table table-bordered" id="tablaRoles">
                    <thead>
                        <tr>
                            <th>Rol</th>
                            <th>Farmacia</th>
                            <th>Unidad de Negocio</th>
                            <th style="width:40px;" class="text-center"></th>
                        </tr>
                    </thead>
                    <tbody>
                        <tr class="text-center text-muted" id="noRolesRow">
                            <td colspan="4">No hay roles encontrados</td>
                        </tr>
                    </tbody>
                </table>
            </div>


            <div class="modal-footer">
                <button class="btn btn-outline-secondary" data-bs-dismiss="modal">Cancelar</button>
                <button id="btnGuardar" class="btn btn-primary">Guardar</button>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script src="https://cdn.jsdelivr.net/npm/choices.js/public/assets/scripts/choices.min.js"></script>
    

    <script>
        let choicesMarcas;

                $('#modalEntidad').on('shown.bs.modal', function () {
            const unidadId = $('#unidadDeNegocioSelect').val();

            // Si ya hay unidad seleccionada, cargar sus marcas
            if (unidadId) {
                const marcasPreseleccionadas = [1]; // estos IDs deben coincidir con los que vienen del backend
                    cargarMarcas(unidadId, marcasPreseleccionadas);
            }
        });

         document.addEventListener('DOMContentLoaded', function () {         

            cargarUnidadesNegocio();         
            document.getElementById("unidadDeNegocioSelect").addEventListener("change", function () {
                const unidadId = this.value;
                if (unidadId) {
                    const marcasPreseleccionadas = [1]; // estos IDs deben coincidir con los que vienen del backend
                    cargarMarcas(unidadId, marcasPreseleccionadas);
                } else {
         
                    const marcasSelect = document.getElementById("marcasSelect");
                    marcasSelect.innerHTML = '';
                }
            });

        });



        $(document).ready(function () {
            cargar();
            cargarUnidadesNegocio();            
            $('#btnNuevo').click(function () {
                limpiar();
                $('#modalTitulo').text('Nuevo usuario');
                $('#modalEntidad').modal('show');
            });



            $('#btnGuardar').click(function () {
                const id = $('#entidadId').val() || 0;
                const datos = {
                    Id: id,
                    Nombre: $('#nombre').val(),
                    ApellidoPaterno: $('#apellidoPaterno').val(),
                    ApellidoMaterno: $('#apellidoMaterno').val(),
                    Email: $('#email').val(),
                    NumeroDeTelefono: $('#telefono').val(),
                    Estatus: $('#estatus').val() === 'true'
                };

                $.ajax({
                    url: '@Url.Action("Guardar", "Usuario")',
                    method: 'POST',
                    contentType: 'application/json',
                    data: JSON.stringify(datos),
                    success: function (r) {
                        if (r.success) {
                            $('#modalEntidad').modal('hide');
                            const mensaje = parseInt(id) === 0
                                ? 'Usuario registrado correctamente'
                                : 'Usuario actualizado correctamente';
                            showAlert(mensaje, 'success');
                            cargar();
                        } else {
                            showAlert(r.error || 'Error al guardar', 'error');
                        }
                    }
                });
            });
        });

        async function cargarUnidadesNegocio() {
            try {
                const url = '@Url.Action("ListarUnidadesNegocio", "Usuario")';
                const response = await fetch(url);
                const result = await response.json();

                if (result.success) {
                    const select = document.getElementById("unidadDeNegocioSelect");
                    select.innerHTML = '<option value="">-- Selecciona una unidad --</option>';

                    result.data.forEach(unidad => {
                        const option = document.createElement("option");
                        option.value = unidad.id;
                        option.text = unidad.nombre;
                        select.appendChild(option);
                    });
                } else {
                    console.error("No se pudieron obtener las unidades de negocio.");
                }

            } catch (error) {
                console.error("Error cargando unidades de negocio:", error);
            }
        }


        // async function cargarMarcas(unidadId) {
        //     try {
        //         const url = '@Url.Action("ListarMarcas", "Usuario")' + `?unidadId=${unidadId}`;
        //         const response = await fetch(url);
        //         const result = await response.json();

        //         const select = document.getElementById("marcasSelect");

        //         // Si ya hay una instancia de Choices, destruirla y limpiar el select
        //         if (choicesMarcas) {
        //             choicesMarcas.destroy();
        //             choicesMarcas = null;
        //         }

        //         select.innerHTML = ''; // Limpiar opciones anteriores

        //         if (result.success) {
        //             result.data.forEach(marca => {
        //                 const option = document.createElement("option");
        //                 option.value = marca.id;
        //                 option.text = marca.nombre;
        //                 select.appendChild(option);
        //             });
        //         }

        //         // Crear nueva instancia con los datos nuevos
        //         choicesMarcas = new Choices(select, {
        //             removeItemButton: true,
        //             placeholderValue: 'Selecciona marcas...',
        //             searchPlaceholderValue: 'Buscar...',
        //         });

        //     } catch (error) {
        //         console.error("Error cargando marcas:", error);
        //     }
        // }

                async function cargarMarcas(unidadId, seleccionadas = []) {
            try {
                const url = '@Url.Action("ListarMarcas", "Usuario")' + `?unidadId=${unidadId}`;
                const response = await fetch(url);
                const result = await response.json();

                const select = document.getElementById("marcasSelect");

                if (choicesMarcas) {
                    choicesMarcas.destroy();
                    choicesMarcas = null;
                }

                select.innerHTML = ''; // Limpiar opciones anteriores

                if (result.success) {
                    result.data.forEach(marca => {
                        const option = document.createElement("option");
                        option.value = marca.id;
                        option.text = marca.nombre;

                        if (seleccionadas.includes(parseInt(marca.id))) {
                            option.selected = true;
                        }

                        select.appendChild(option);
                    });
                }

                choicesMarcas = new Choices(select, {
                    removeItemButton: true,
                    placeholderValue: 'Selecciona marcas...',
                    searchPlaceholderValue: 'Buscar...',
                });

            } catch (error) {
                console.error("Error cargando marcas:", error);
            }
        }



        function cargarSelectorRoles() {
                $.get('@Url.Action("Listar", "Rol")', function (r) {
                    const select = $('#selectRol');
                    select.empty();
                    select.append('<option value="">-- Selecciona un rol --</option>');
                    r.data.forEach(rol => {
                        select.append(`<option value="${rol.id}">${rol.nombre}</option>`);
                    });
                });
            }

        function cargar() {
            $.get('@Url.Action("Listar", "Usuario")', function (r) {
                if (r.success) {
                    const tabla = $('#tablaDatos');
                    if ($.fn.DataTable.isDataTable(tabla)) {
                        tabla.DataTable().destroy();
                    }

                    const tbody = tabla.find('tbody');
                    tbody.empty();
                    r.data.forEach(u => {
                        tbody.append(`<tr>
                            <td>${u.id}</td>
                            <td>${u.nombre} ${u.apellidoPaterno}</td>
                            <td>${u.email}</td>
                            <td>${u.numeroDeTelefono ?? ''}</td>
                            <td>${u.estatus ? 'Activo' : 'Inactivo'}</td>
                            <td class="text-center">
                                <button class="btn btn-sm" style="background-color:#00ab8e; color:white;" onclick="editar(${u.id})"><i class="bi bi-pencil"></i></button>
                                <button class="btn btn-sm" style="background-color:#79b828; color:white;" onclick="eliminar(${u.id})"><i class="bi bi-trash"></i></button>
                            </td>
                        </tr>`);
                    });

                    tabla.DataTable({
                        pageLength: 5,
                        lengthMenu: [5, 10, 25, 50, 100],
                        language: {
                            url: '//cdn.datatables.net/plug-ins/1.13.6/i18n/es-ES.json'
                        }
                    });
                }
            });
        }

        function editar(id) {
            $.get('@Url.Action("Obtener", "Usuario")', { id }, function (r) {
                if (r.success) {
                    const u = r.data;
                    $('#modalTitulo').text('Editar usuario');
                    $('#entidadId').val(u.id);
                    $('#nombre').val(u.nombre);
                    $('#apellidoPaterno').val(u.apellidoPaterno);
                    $('#apellidoMaterno').val(u.apellidoMaterno);
                    $('#email').val(u.email);
                    $('#telefono').val(u.numeroDeTelefono);
                    $('#estatus').val(u.estatus ? 'true' : 'false');
                    $('#modalEntidad').modal('show');
                } else {
                    showAlert(r.error || 'No se pudo cargar', 'error');
                }
            });

            cargarSelectorRoles();
        }

        function eliminar(id) {
            confirmAction('¿Deseas eliminar este usuario?').then(function (ok) {
                if (!ok) return;
                $.post('@Url.Action("Eliminar", "Usuario")', { id }, function (r) {
                    if (r.success) {
                        cargar();
                    } else {
                        showAlert(r.error || 'Error al eliminar', 'error');
                    }
                });
            });
        }

        function limpiar() {
            cargarSelectorRoles();
            $('#entidadId').val('');
            $('#nombre').val('');
            $('#apellidoPaterno').val('');
            $('#apellidoMaterno').val('');
            $('#email').val('');
            $('#telefono').val('');
            $('#estatus').val('true');
            $('#selectRol').val('');
            $('#tablaRoles tbody').html('<tr class="text-center text-muted" id="noRolesRow"><td colspan="4">No hay roles encontrados</td></tr>');

            const marcasSelect = document.getElementById("marcasSelect");
            marcasSelect.innerHTML = '';

            if (choicesMarcas) {
                choicesMarcas.destroy();
                choicesMarcas = null;
            }

            choicesMarcas = new Choices(marcasSelect, {
                removeItemButton: true,
                placeholderValue: 'Selecciona marcas...',
                searchPlaceholderValue: 'Buscar...',
            });
        }
        function agregarRolARelacion(rol, farmacia, unidadNegocio) {
            $('#noRolesRow').hide();
            $('#tablaRoles tbody').append(`
                <tr>
                    <td>${rol}</td>
                    <td>${farmacia}</td>
                    <td>${unidadNegocio}</td>
                    <td class="text-center">
                        <button type="button" class="btn btn-sm btn-danger" onclick="$(this).closest('tr').remove(); controlarMensajeSinRoles();">
                            <i class="bi bi-x"></i>
                        </button>
                    </td>
                </tr>
            `);
        }

        function controlarMensajeSinRoles() {
            if ($('#tablaRoles tbody tr').length === 1) {
                $('#noRolesRow').show();
            }
        }
    </script>
}
